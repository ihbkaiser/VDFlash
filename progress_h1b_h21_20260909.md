# Progress log: H1b and H2.1

## 2026-09-09

- Started a separate execution plan for H1b and H2.1.
- No new GPU inference has been claimed yet; model/runtime capability is being
  checked before spending the full dataset budget.
- R1 passed: both GPUs are idle, Qwen2-VL/MSD snapshots are cached, and the VDC
  manifest validates. Starting the 3-sample Natural H2.1 pilot before full VDC50.
- R2 passed after two instrumentation fixes: the probe captures per-layer
  attention rather than the MSD runtime trace, and KL compares the first draft
  LM-head distribution under real versus zero visual values.
- R3 passed: the 3–5 sample pilots for H1b and H2.1 completed.
- R4 passed: H1b full VDC50 and MVBench inference completed with 600 and
  12,000 valid rows respectively; H2.1 full VDC50 Natural and Answer-hint
  inference completed with 150 rows each.
- Coverage verification passed: H1b has no error rows, all expected VDC/MVBench
  sample IDs are present, and each sample has 12 rows; H2.1 has 150/150 paired
  keys and no error rows.
- R5 analysis passed at the exploratory/diagnostic level. Attention mass
  shifts toward text after visual reduction/deletion, while the value hook
  changes draft logits without changing captured attention. H2.1 acceptance
  increases after deletion in both prompt variants; answer-hint does not
  eliminate that effect.
- Remaining limitations are recorded: the hidden-state comparison against the
  training distribution needs unavailable training data/statistics, and raw
  probability KL is numerically saturated at zero for the current temperature.
- Fresh verification: the seven focused H1b/H2.1 tests pass; changed Python
  modules compile and `git diff --check` passes. The repository-wide pytest
  command remains blocked by three pre-existing tests importing the missing
  `src.analyze.Whether_they_are_appliable_for_dDrafter` package.

## 2026-09-10 — report reconstruction started

- The requested deliverable is now being reorganized by research question and
  hypothesis rather than by experiment ID.
- The visualization phase will use a new tested report-level plotting module;
  figures will be written under `results/hypothesis_report_figures_20260910/`
  and embedded beside the corresponding result tables.
- Added `src/analyze/plot_hypothesis_report.py` with tested JSONL/CSV loaders,
  hypothesis-level summaries, six report figures in PNG/PDF, and a source
  manifest. The MVBench pilot file was explicitly excluded from the full-run
  aggregation; the manifest now records 12,600 H1b rows and 300 H2.1 rows.
- Reconstructed `markdown/bao_cao_tong_hop_thuc_nghiem_hypothesis_20260910.md`
  by Q1/H1, Q2/H2.1 and Q3/H3.1–H3.2. Each hypothesis now has predictions,
  mapped experiments, tables, figures, decision and missing-data notes.
- Final verification passed: 12 focused tests, module compilation, Markdown
  table shape check, all local report/artifact links, figure non-empty checks,
  and `git diff --check`.

## 2026-09-11 — completeness revision started

- Review found that H1a mentioned E6 but only displayed the E4 figure; E6 was
  shown only under H1b. The revision will repeat E6's attention/value tables
  and figures inside H1a, as requested.
- The same audit will expand E1–E5, E7–E9 with the available sample counts,
  standard deviations/confidence intervals or raw pilot values, and retain
  explicit incomplete-status tables for E8/E10.
- Repeated E6 under H1a with full VDC50/MVBench attention and value-ablation
  tables plus both corresponding figures. Expanded E1–E4, E5, E7 and E8 with
  available descriptive statistics; explicitly marked unavailable H3.1 bin
  denominators/CI rather than inventing them.
- Static checks passed after the revision, and the figure generator still
  records 12,600 H1b rows and 300 H2.1 rows.
- Final completeness revision verification passed: report links, image links,
  table-shape scan, figure regeneration, 12 focused tests, compilation and
  `git diff --check`.

## 2026-09-11 — long-table presentation revision started

- The next revision will move long E1–E5, E8 and E9 tables into plots. Compact
  summaries will remain in the report, while raw/full statistics stay linked
  to the recorded CSV/JSONL artifacts.

## 2026-09-11 — long-table presentation revision completed

- Replaced the long E4, E6 attention, E1/E2, E8 and E9 tables in the report
  with embedded plots and links to their complete CSV/JSONL sources. The
  report now has at most six contiguous Markdown table lines; remaining
  tables are compact decision/statistics summaries.
- Added the artifact-backed E5 zero-value pilot figure, including both
  attention policies and the available Real/Zero acceptance pilot points.
- Moved H3.1 legacy DFlash position rates to
  `results/hypothesis_report_figures_20260910/h3_dflash_position_acceptance.csv`
  so figure regeneration no longer parses presentation tables from the report.
- Regenerated the figure manifest: 12,600 H1b rows, 300 H2.1 rows and 14
  PNG/PDF figure files. Focused tests pass (14 tests), the plotting module
  compiles, all report/artifact links resolve, and `git diff --check` passes.
