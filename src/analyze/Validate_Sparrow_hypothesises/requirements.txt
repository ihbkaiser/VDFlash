# Python 3.10 / CUDA 12.1 environment for the MSD/Sparrow validation suite.
# Keep this environment separate from src/analyze/requirements.txt (Qwen3.5).
torch==2.1.2
torchvision==0.16.2
# Qwen2-VL support; MSD/EAGLE remains compatible with this release.
transformers==4.49.0
accelerate==1.3.0
bitsandbytes>=0.45
# bitsandbytes needs the CUDA 12 sparse runtime on hosts without a system toolkit.
nvidia-cusparse-cu12==12.1.0.106
qwen-vl-utils>=0.0.10
av>=12
Pillow>=10
PyYAML>=6
numpy>=1.26,<2
matplotlib>=3.8
pytest>=8
ijson>=3.3,<4
pandas>=2.1,<3
pyarrow>=14,<24
